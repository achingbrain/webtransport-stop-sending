{
  "name": "go-webtransport-readable-never-ends",
  "version": "1.0.0",
  "description": "WebTransport streams should still be readable after the server sends STOP_SENDING",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js"
  }
}
